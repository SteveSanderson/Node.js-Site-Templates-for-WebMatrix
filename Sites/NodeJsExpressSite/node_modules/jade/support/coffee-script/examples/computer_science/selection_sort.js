(function() {
  var selection_sort;
  selection_sort = function(list) {
    var i, j, len, min, _ref, _ref2;
    len = list.length;
    for (i = 0; 0 <= len ? i < len : i > len; 0 <= len ? i++ : i--) {
      min = i;
      for (j = _ref = i + 1; _ref <= len ? j < len : j > len; _ref <= len ? j++ : j--) {
        if (list[j] < list[min]) {
          min = j;
        }
      }
      if (i !== min) {
        _ref2 = [list[min], list[i]], list[i] = _ref2[0], list[min] = _ref2[1];
      }
    }
    return list;
  };
  puts(selection_sort([3, 2, 1]).join(' ') === '1 2 3');
  puts(selection_sort([9, 2, 7, 0, 1]).join(' ') === '0 1 2 7 9');
}).call(this);
